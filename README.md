# Face-Identification
A custom face identification system built from scratch using PyTorch. It detects and recognizes faces by generating deep embeddings through a ResNet-based network and ArcFace loss, enabling accurate person identification without relying on pretrained face recognition models. Without OpenCV.
